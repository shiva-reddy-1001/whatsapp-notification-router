# Python 3.9-compatible runtime dependencies for the Message Notification Router.
# Keep the standard library as the default for CSV I/O; these packages enable
# configured providers, retrieval, and multimodal processing when available.

# Configuration and remote LLM provider
python-dotenv==1.2.1
openai==1.109.1

# Local Ollama provider
ollama==0.6.2

# Deterministic similarity retrieval and image/OCR extraction
rapidfuzz==3.13.0
Pillow==11.3.0
pytesseract==0.3.13

# Local voice-note transcription. The application must gracefully fall back
# when this optional capability or its model files are unavailable.
faster-whisper==1.2.1
